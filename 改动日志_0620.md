# EasyCod 改动日志 — 2025-06-20

---

## 一、核心 Bug 修复

### 1. 手机端无法写入 Supabase（RLS 42501 权限拒绝）

**根因**：手机端 Supabase auth session（`sb-*-auth-token`）丢失，导致 `auth.uid()` 为 null，RLS 策略拒绝 INSERT。之前登录只保存了用户信息（App 层身份），从未保存 Supabase token（数据库层身份）。

**修复**（文件：`login.js`、`app.js`、`db-writer.js`）：
- 登录成功时显式保存 `_supabaseSession`（含 `access_token` + `refresh_token`）到 localStorage
- App 启动时 `checkSession()` 恢复 Supabase session
- 写入数据库前检测 session 是否过期/丢失，自动从 Store 恢复

### 2. DB 写入失败未标记水印

**根因**：`upsertSample` 返回 `false`（非抛异常）时，不会进入 `catch` 分支，`_uploadFailed` 始终为 `false`，导致失败的样板没有 FALSE 水印。之前的"DB upsertSample SUCCESS"日志是假成功（没检查返回值）。

**修复**（文件：`upload-manager.js`）：
- `upsertSample` 返回 `false` 时设置 `_uploadFailed = true`
- 保存 Store，toast 提示"可稍后重试"

### 3. 手机端录入样板耗时过长（~10 秒 → ~7 秒）

**根因分析**（通过分步计时定位）：

| 步骤 | 修复前耗时 | 问题 |
|------|-----------|------|
| 压全（600px 压缩） | 48ms | 正常 |
| 传全（上传 Supabase） | 5404ms | 网络瓶颈，无法减少 |
| **缩（缩略图压缩）** | **2530ms** | 从 Supabase 下载刚上传的图再压缩，完全浪费 |
| 传缩（上传缩略图） | 1696ms | 正常 |
| DB 写入 | 223ms | 正常 |

- `compressImage(签名URL, 200)` → `fetch` Supabase 下载原图 → 再压缩 → 浪费 2.5 秒
- `renderSamples` 全量 `innerHTML` 替换 126 张卡片 → ~300-800ms

**修复 A**（文件：`upload-manager.js`）：
- 缩略图压缩改用内存中的 `compressed` data URL 代替网络 URL，省 2.5 秒

**修复 B**（文件：`app.js`）：
- 新建样板改为增量追加（`insertAdjacentHTML`），不动已有 DOM，省 ~300-800ms
- 编辑已有样板仍走全量渲染

### 4. 删除同步与校验逻辑冲突

**根因**：之前的"合并模式"修复了 silentSync 吞掉新样板的问题，但导致其他端删除的样板无法同步到本端。`_markLocalOnlySamples` 把所有"本地有、DB 无"的都标记水印，包括被别端正常删除的。

**修复**（文件：`app.js`）：
- `imageUrl` 是 Supabase URL → 曾被同步过 → 被别端删了 → 从本地移除
- `imageUrl` 是 data URL 或无图 → 从未上传成功 → 标记 `_uploadFailed` 加水印

---

## 二、安全增强

### 5. samples 表 RLS 策略收紧

- **删除** `Public can view samples` 策略（`SELECT` 条件为 `true`，任何人无需登录可读）
- 现在全部操作（SELECT/INSERT/UPDATE/DELETE）都需要 `auth.uid() = user_id`

### 6. 批量删除增加密码验证

- 批量删除按钮点击后弹出与单条删除一致的密码输入弹窗（`deleteConfirmModal`）
- 输入正确密码后才能批量删除（含 DB 记录 + Storage 图片 + 本地缓存）

---

## 三、新增功能

### 7. 切换品类自动比对数据库

- 每次 `renderSamples` 异步比对本地 Store vs Supabase DB
- 本地有但 DB 无的样板：自动标记水印（data URL）或从本地移除（Supabase URL）

### 8. 失败样板 1 天自动清理

- App 启动时执行 `Store._cleanExpiredFailedSamples()`
- `_uploadFailed === true` 且 `created_at` 超过 24 小时的样板从 localStorage 移除

### 9. 详情页缩略图优化

- 样板详情页初始显示 `thumbnailUrl`（200px 缩略图，秒加载）
- 点击缩略图弹出 `imageUrl` 原图大图

---

## 四、清理

### 10. 调试代码移除

- 移除 ~30 条 `console.log`/`console.warn` 调试日志
- 移除 eruda 诊断面板
- 移除 diagnostic toast（UI 右下角的调试信息）

### 11. 批量重试按钮移除

- 删除 HTML 按钮、CSS 样式、JS 事件、`updateBatchBtns` 相关逻辑

### 12. 录入"保存成功"toast 移除

- 进度 toast 已完整反映上传状态，无需额外 toast

### 13. 墙纸墙布品类数据清空

- `DELETE FROM samples WHERE project_id = '墙纸墙布的项目ID'`

### 14. 分步计时诊断 toast 移除

- 移除 saveSample 计时 toast（压缩/存储/渲染/入队）
- 移除 uploadToStorage 分步 toast（full/thumb 耗时）
- 移除 UploadManager 汇总 toast（压/传全/缩/传缩/DB/总）

---

## 五、涉及文件

| 文件 | 改动 |
|------|------|
| `src/js/login.js` | 登录成功时保存 `_supabaseSession` |
| `src/js/app.js` | session 恢复、增量渲染、`_markLocalOnlySamples` 分流、失败过期清理、批量删除弹窗、缩略图替换、调试清理 |
| `src/js/app/upload-manager.js` | 缩略图用内存 data URL、DB 失败标记、移除计时 toast |
| `src/js/app/store.js` | `_cleanExpiredFailedSamples` 过期清理 |
| `src/js/app/db-writer.js` | 写入前 session 恢复逻辑 |
| `src/html/easycod.html` | 批量删除按钮、密码弹窗复用 |
| `src/css/style.css` | 按钮样式统一 |

---

## 六、已知余留

- 手机端 Supabase Storage 上传耗时 ~5 秒，网络瓶颈，暂无优化空间
- `uploadImageToStorage` 函数中 `tBlob`、`tUp`、`tSign` 等变量已无引用（仅作历史追踪）
