# 材料样板管理系统 - 用户外观手册

> 基于现有代码（index.html + app.js + style.css）生成的用户外观与交互手册
> 版本：v1.0 | 日期：2026-06-07

---

## 目录

1. [整体布局与导航](#1-整体布局与导航)
2. [登录页面](#2-登录页面)
3. [仪表盘](#3-仪表盘)
4. [类别管理（卡片/列表双视图）](#4-类别管理)
5. [样板管理](#5-样板管理) ⚠️ **标签样式维持原状**
6. [样板详情](#6-样板详情)
7. [标签打印](#7-标签打印)
8. [信息页面](#8-信息页面)
9. [订单管理](#9-订单管理)
10. [申请管理](#10-申请管理)
11. [打卡管理](#11-打卡管理)
12. [弹窗系统](#12-弹窗系统)
13. [全局 UI 元素](#13-全局-ui-元素)

---

## 1. 整体布局与导航

### 1.1 整体布局结构

```
┌─────────────────────────────────────────────────────┐
│  <aside class="sidebar">                            │
│  ┌──────────────────┐  ┌──────────────────────────┐ │
│  │  材料样板管理     │  │  <main class="container">  │ │
│  │  (sidebar-title)  │  │                          │ │
│  ├──────────────────┤  │  #dashboardSection        │ │
│  │  <img src="src/icon/user.svg" width="14"> 用户名 │  │  #projectsSection        │ │
│  │  (sidebar-user)   │  │  #samplesSection         │ │
│  ├──────────────────┤  │  #labelsSection           │ │
│  │  <img src="src/icon/folder.svg" width="14"> 类别 │  │  #sampleDetailSection    │ │
│  │  <img src="src/icon/info.svg" width="14"> 信息 │  │  #infoSection             │ │
│  │  <img src="src/icon/clipboard-text.svg" width="14"> 订单管理 │  │  #ordersSection           │ │
│  │  <img src="src/icon/notebook.svg" width="14"> 申请管理 │  │  #applySection            │ │
│  │  <img src="src/icon/clock.svg" width="14"> 打卡管理 │  │  #clockSection            │ │
│  ├──────────────────┤  │                          │ │
│  │  <img src="src/icon/sign-out.svg" width="14"> 退出 │  │                          │ │
│  └──────────────────┘  └──────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 1.2 侧边栏导航（sidebar）

| 元素 | ID | 行为 |
|------|-----|------|
| 标题"材料样板管理" | `.sidebar-title` | 点击跳转仪表盘 |
| 用户名区域 | `#sidebarUser` | 显示当前用户名，演示模式带徽章 |
| 类别 | `#navProjects` | 切换至类别管理页，`renderProjects()` |
| 信息 | `#infoBtn` | 切换至信息页，`renderInfoView()` |
| 订单管理 | `#navOrders` | 切换至订单管理页，`renderOrders()` |
| 申请管理 | `#navApply` | 切换至申请管理页，`renderApply()` |
| 打卡管理 | `#navClock` | 切换至打卡管理页，`renderClock()` |
| 退出 | `#logoutBtn` | 退出登录，红色文字，固定在底部 |

**视觉样式**：
- 侧边栏固定宽度 200px，白色背景，全高粘性定位
- `.sidebar-link`：正常态灰色文字，hover 浅蓝背景+蓝色文字
- `.sidebar-link.active`：蓝色填充背景（`--primary: #007aff`），白色文字
- `.sidebar-logout`：红色文字（`--danger: #ff3b30`）
- `margin-top: auto` 使退出按钮固定在底部

### 1.3 响应式布局

| 断点 | 变化 |
|------|------|
| <= 768px | 侧边栏变为顶部导航；卡片网格变为 1 列；表格内联布局变纵向 |
| <= 480px | 样板网格变为 2 列；内边距和字号缩小 |
| >= 769px | 桌面布局，卡片 1:1 宽高比 |

---

## 2. 登录页面

### 2.1 布局

```
┌─────────────────────────────────────┐
│         ┌─────────────────┐         │
│         │                 │         │
│         │  EeaySamplistCod│         │
│         │                 │         │
│         │  [Email 输入框]  │         │
│         │  [Password 输入]│         │
│         │                 │         │
│         │  [Login 按钮]   │         │
│         │                 │         │
│         └─────────────────┘         │
└─────────────────────────────────────┘
```

### 2.2 元素清单

| 元素 | 类型 | 说明 |
|------|------|------|
| Email 输入 | `#loginEmail` | 邮箱格式输入框 |
| Password 输入 | `#loginPassword` | 密码输入框 |
| Login 按钮 | `#loginForm button[type=submit]` | 主色全宽按钮，触发 Supabase 登录 |
| 演示模式按钮 | `#demoLoginBtn` | 次要按钮，仅 `DEMO_MODE=true` 时显示 |

### 2.3 交互逻辑
- 表单提交 → `handleLogin()` → Supabase Auth → 成功则 `showApp()`，失败吐司错误
- 演示模式 → `handleDemoLogin()` → 创建演示用户 → 种子演示数据 → `showApp()`

---

## 3. 仪表盘

### 3.1 布局

```
┌──────────────────────────────────────────┐
│  <img src="src/icon/chart-pie-slice.svg" width="16"> 仪表盘（section-header） │
├──────────────────────────────────────────┤
│  ┌──────┐  ┌──────┐  ┌──────────────┐   │
│  │ 已录入 │  │ 当前  │  │ 今日邀请码    │   │
│  │ 样板数 │  │ 品类数│  │  X  X  X  X  │   │
│  │  12   │  │  3   │  │  X  X (复制) │   │
│  └──────┘  └──────┘  └──────────────┘   │
│                                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 订单状态  │ │ 申请类型  │ │ 打卡角色  │ │
│  │ (甜甜圈)  │ │ (甜甜圈)  │ │ (甜甜圈)  │ │
│  └──────────┘ └──────────┘ └──────────┘ │
└──────────────────────────────────────────┘
```

### 3.2 元素清单

| 元素 | 说明 |
|------|------|
| 已录入样板数 | 数字动画计数（`_animateNumbers`，800ms easeOutCubic） |
| 当前品类数 | 数字动画计数 |
| 今日邀请码 | 6 位随机码，翻牌动画（`_animateFlipCode`，1200ms），点击复制 |
| 订单状态甜甜圈 | 按 status 分组的 conic-gradient 图表 |
| 申请类型甜甜圈 | 按 type 分组的 conic-gradient 图表 |
| 打卡角色甜甜圈 | 按 companyType 分组的 conic-gradient 图表 |

### 3.3 交互逻辑
- 数字加载时从 0 动画增长到目标值
- 邀请码逐个字母翻牌揭晓，点击复制到剪贴板
- 甜甜圈图带从灰色到目标色的渐变动画

---

## 4. 类别管理

### 4.1 布局

```
┌───────────────────────────────────────────────────┐
│  类别                          [<img src="src/icon/arrows-clockwise.svg" width="14"> 刷新] │
├───────────────────────────────────────────────────┤
│ [<img src="src/icon/magnifying-glass.svg" width="14"> 搜索...] [全部品牌▼] [所属类别▼] [是否集采▼] │
│ [集采范围▼] [查询]          [<img src="src/icon/squares-four.svg" width="14">卡片/<img src="src/icon/list.svg" width="14">列表] [<img src="src/icon/plus.svg" width="14">新建] │
├───────────────────────────────────────────────────┤
│                                                   │
│  卡片模式：┌─────┐ ┌─────┐ ┌─────┐                │
│           │名称  │ │名称  │ │名称  │                │
│           │品牌  │ │品牌  │ │品牌  │                │
│           │N个   │ │N个   │ │N个   │                │
│           │样板  │ │样板  │ │样板  │                │
│           ├─────┤ ├─────┤ ├─────┤                │
│           │查看  │ │查看  │ │查看  │                │
│           │编辑  │ │编辑  │ │编辑  │                │
│           │删除  │ │删除  │ │删除  │                │
│           └─────┘ └─────┘ └─────┘                │
│                                                   │
│  列表模式：┌────────────── 表格 ──────────────┐   │
│           │ 序号│图片│编号│名称│...│操作     │   │
│           ├───────────────────────────────────┤   │
│           │  1  │ <img src="src/icon/eye.svg" width="14"> │... │... │...│查看/编辑│   │
│           └───────────────────────────────────┘   │
└───────────────────────────────────────────────────┘
```

### 4.2 工具栏元素

| 元素 | ID | 说明 |
|------|-----|------|
| 搜索输入 | `#projectSearchInput` | 模糊匹配编号/品名/品牌，实时过滤 |
| 品牌筛选 | `#projectBrandFilter` | 下拉框，动态填充 |
| 类别筛选 | `#projectCategoryFilter` | 下拉框，按项目名 |
| 集采筛选 | `#projectProcFilter` | 下拉框：不限/集采/非集采 |
| 范围筛选 | `#projectRangeFilter` | 下拉框：不限/范围内/范围外 |
| 查询按钮 | `#projectQueryBtn` | 点击触发重新渲染 |
| 刷新按钮 | `#projectRefreshBtn` | 从数据库重新加载数据 |
| 视图切换 | `#projectViewToggle` | 卡片/列表双模式 |
| 新建类别 | `#createProjectBtn` | 打开创建类别弹窗 |

**工具栏视觉**：flex wrap 布局，白色背景带边框，内边距 12px 16px，内部元素高 32px。

### 4.3 视图切换按钮

```
┌──────────┬──────────┐
│  <img src="src/icon/squares-four.svg" width="14"> 卡片 │  <img src="src/icon/list.svg" width="14"> 列表 │
│  (active) │          │
└──────────┴──────────┘
```

- `.toggle-btn:first-child` 左半圆角，`.toggle-btn:last-child` 右半圆角
- `.active`：蓝色背景（`--primary`）+ 白色文字
- 点击切换后重新渲染 `renderProjects()`

### 4.4 卡片模式（默认）

**卡片网格**（`.card-grid`）：CSS Grid，`repeat(auto-fill, minmax(180px, 1fr))`，10px 间距

**每个卡片**（`.card`）：
```
┌──────────────────────┐
│  类别名称    [品牌]   │  ← .card-title + .card-meta-brand
│  描述文字            │  ← .card-text（可选）
│  N 个样板  [集采徽章]  │  ← .card-meta
├──────────────────────┤
│  [查看样板] [<img src="src/icon/pencil-simple.svg" width="12"> 编辑] [<img src="src/icon/trash.svg" width="12"> 删除]│  ← .card-actions
└──────────────────────┘
```

**集采徽章**：
- `.card-badge.procurement` — 绿色圆形
- `.card-badge.non-procurement` — 红色叉形

**按钮事件**：
| 按钮 | 行为 |
|------|------|
| 查看样板 | `renderSamples(projectId)` → `showView('samples')` |
| 编辑 | 打开项目编辑弹窗，预填数据 |
| 删除 | `promptDelete('project', ...)` 密码确认删除 |

**空状态**：居中显示 <img src="src/icon/file.svg" width="24"> 图标 + "暂无类别，点击右上角"<img src="src/icon/plus.svg" width="12"> 新建类别"开始"

### 4.5 列表模式

**表格**（`.data-table`）：展示所有样品的扁平列表

| 列名 | 内容 |
|------|------|
| 序号 | 自增数字 |
| 图片 | 32x32 缩略图，可点击放大 |
| 编号 | 等宽字体的编码 |
| 名称 | 样板名称 |
| 型号 | 型号文字 |
| 品牌 | 品牌文字 |
| 所属类别 | 关联的类别名称 |
| 是否集采 | 绿色"集采"/红色"非集采" |
| 集采范围 | 绿"范围内"/橙"范围外"/红"范围外" |
| 创建时间 | 格式化日期时间 |
| 操作 | 查看 / 编辑 / 删除（内联 onClick） |

**筛选逻辑**：工具栏筛选条件过滤 `samples` 数据源

---

## 5. 样板管理

> ⚠️ **此页面的标签/标题区域样式维持原状，不做修改。**

### 5.1 布局

```
┌───────────────────────────────────────────────────┐
│  当前类别名称                    [<img src="src/icon/arrow-left.svg" width="14"> 返回类别] │
│  (currentProjectName)          (backToProjects)   │
├───────────────────────────────────────────────────┤
│ [<img src="src/icon/magnifying-glass.svg" width="14"> 搜索...]  [全选] [<img src="src/icon/printer.svg" width="14"> 批量打印] [<img src="src/icon/pencil-simple.svg" width="14"> 编辑类别] [<img src="src/icon/plus.svg" width="14"> 录入] │
├───────────────────────────────────────────────────┤
│                                                   │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐             │
│  │ <img src="src/icon/check.svg" width="14"> <img src="src/icon/eye.svg" width="14"> │ │ <img src="src/icon/x.svg" width="14"> <img src="src/icon/eye.svg" width="14"> │ │ <img src="src/icon/x.svg" width="14"> <img src="src/icon/eye.svg" width="14"> │ │ <img src="src/icon/x.svg" width="14"> <img src="src/icon/eye.svg" width="14"> │ │
│  │ 名称  │ │ 名称  │ │ 名称  │ │ 名称  │          │
│  │ 型号  │ │ 型号  │ │ 型号  │ │ 型号  │          │
│  │ 编号  │ │ 编号  │ │ 编号  │ │ 编号  │          │
│  │ 集采  │ │ 集采  │ │ 集采  │ │ 集采  │          │
│  │状态   │ │状态   │ │状态   │ │状态   │          │
│  ├──────┤ ├──────┤ ├──────┤ ├──────┤             │
│  │详编删│ │详编删│ │详编删│ │详编删│             │
│  └──────┘ └──────┘ └──────┘ └──────┘             │
│                                                   │
│  （标签）按钮位于图片右上角半透明                   │
│  （标签）按钮点击→单标签预览弹窗                   │
└───────────────────────────────────────────────────┘
```

### 5.2 头部区域（**维持原状**）

| 元素 | ID | 说明 |
|------|-----|------|
| 当前类别名称 | `#currentProjectName` | `<h2>` 显示当前项目的名称 |
| 返回按钮 | `#backToProjects` | 样式为 `.back-btn`，内容 `<img src="src/icon/arrow-left.svg" width="14"> 返回类别` |

**样式**（不修改）：
- `.section-header`：flex 布局，space-between 排列
- `#currentProjectName`：`<h2>` 标题样式
- `#backToProjects`：`.back-btn` 类，带 `<img src="src/icon/arrow-left.svg" width="14">` 箭头前缀

### 5.3 工具栏

| 元素 | ID | 说明 |
|------|-----|------|
| 搜索输入 | `#sampleBrandFilter` | 实时过滤编号/名称/型号/范围 |
| 全选按钮 | `#selectAllBtn` | 切换全选/取消全选 |
| 批量打印 | `#batchPrintBtn` | 选中 N 个时启用，显示"批量打印 (N)" |
| 编辑类别 | `#editProjectSampleBtn` | 打开项目编辑弹窗 |
| 录入样板 | `#createSampleBtn` | 打开样板录入弹窗 |

### 5.4 样板卡片网格

**网格**（`.sample-grid`）：CSS Grid `repeat(auto-fill, minmax(150px, 1fr))`，10px 间距

**每个样板卡片**（`.sample-card`）：
```
┌──────────────┐
│ <img src="src/icon/x.svg" width="12">      [<img src="src/icon/tag.svg" width="12"> 标签] │  ← checkbox左上角，标签按钮右上角(半透明)
│   ┌──────┐   │
│   │ 图片  │   │  ← 1:1 比例，object-fit: cover
│   │ 或占位│   │  ← 无图时显示名称前两字
│   └──────┘   │
│  名称  型号   │
│  编号        │
│  集采状态(色) │
├──────────────┤
│ [详情] [编辑] [删除]│
└──────────────┘
```

**图片占位符**：无图片时显示样板名称前两个汉字的首字母缩写，浅灰背景

**集采范围颜色**：
| 状态 | 颜色 |
|------|------|
| 范围内 | 绿色 `var(--success)` |
| 范围外 | 橙色 `var(--warning)` |
| 非集采 | 红色 `var(--danger)` |

**按钮事件**：
| 按钮 | 行为 |
|------|------|
| 标签（图片右上角） | `showSingleLabel(sampleId)` → 单标签预览弹窗 |
| 详情 | `showSampleDetail(sampleId)` → 样板详情页 |
| 编辑 | `editSample(sample)` → 编辑弹窗 |
| 删除 | `confirm()` 简单确认弹窗 |

### 5.5 多选/批量操作

- `this.selectedSamples`（Set）维护选中的样板 ID 集合
- 全选按钮切换选中状态，文字在"全选"/"取消全选"切换
- 批量打印按钮选中 0 个时 disabled，选中 N 个时启用并显示数量
- 批量打印 → `renderLabels()` + `showView('labels')` + 自动调 `window.print()`

---

## 6. 样板详情

### 6.1 布局

```
┌────────────────────────────────────────────┐
│  [<img src="src/icon/arrow-left.svg" width="14"> 返回] │
├────────────────────────────────────────────┤
│                                            │
│  ┌──────────┬──────────┬──────────┬──────┐ │
│  │ 名称     │ 编号     │ 类别     │ 型号  │ │
│  ├──────────┼──────────┼──────────┼──────┤ │
│  │ 品牌     │ 规格     │ 颜色     │ 材质  │ │
│  ├──────────┴──────────┴──────────┴──────┤ │
│  │ 是否集采: 绿色"集采" / 红色"非集采"    │ │
│  │ 集采时间: 2026-01 ~ 2026-12           │ │
│  │ 集采范围: 绿"范围内" / 橙"范围外"      │ │
│  ├───────────────────────────────────────┤ │
│  │ 创建时间: ...   更新时间: ...          │ │
│  ├───────────────────────────────────────┤ │
│  │ 描述（占2列）                          │ │
│  ├───────────────────────────────────────┤ │
│  │ 图片（可点击放大）                     │ │
│  ├───────────────────────────────────────┤ │
│  │ 二维码（canvas + URL文字）             │ │
│  └───────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

### 6.2 元素清单

| 元素 | 说明 |
|------|------|
| 返回按钮 | `#backFromDetail` → 返回样板列表或项目列表 |
| 名称/编号/类别/型号 | 文本展示 |
| 品牌/规格/颜色/材质 | 文本展示 |
| 集采状态 | 绿色<img src="src/icon/check.svg" width="12">集采 / 红色<img src="src/icon/x.svg" width="12">非集采 |
| 集采时间 | 年月起止范围 |
| 集采范围 | 绿/橙/红三色 |
| 创建/更新时间 | 格式化日期时间 |
| 描述 | 占 2 列网格的文本区域 |
| 图片 | 点击放大（`showImagePreview()`） |
| 二维码 | canvas 绘制 + 下方 URL 文字 |

### 6.3 交互
- 图片点击 → 全屏深色背景图片查看器（z-index: 10000），点击背景关闭
- 返回按钮根据上下文判断去向（列表视图 / 样板列表 / 项目列表）

---

## 7. 标签打印

### 7.1 布局

```
┌──────────────────────────────────────────────┐
│  [<img src="src/icon/arrow-left.svg" width="14"> 返回]  标签打印         [<img src="src/icon/printer.svg" width="14"> 打印全部标签] │
├──────────────────────────────────────────────┤
│                                              │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐    │
│  │深色卡 │  │深色卡 │  │深色卡 │  │深色卡 │    │
│  │片+QR  │  │片+QR  │  │片+QR  │  │片+QR  │    │
│  └──────┘  └──────┘  └──────┘  └──────┘    │
│                                              │
│  每个标签附带竖向黑色胶囊（颜色圆点+编码）     │
│                                              │
└──────────────────────────────────────────────┘
```

### 7.2 每个标签的结构

```
┌─────────────────────┐  ┌──────┐
│  类别名称 [集采徽章]  │  │ ●   │
│  样板名称            │  │ 编码 │
│  型号: XXX           │  │ 竖向 │
│  编号: XXX           │  │ 文字 │
│  品牌: XXX           │  │      │
│  是否集采: 是/否      │  └──────┘
│  集采时间: X ~ X     │
│  范围内外: 范围内/外  │
│  ┌─────────────┐    │
│  │  QR Code    │    │
│  └─────────────┘    │
└─────────────────────┘
```

### 7.3 视觉样式
- 深色背景（`#1d1d1f`），白色文字
- 二维码 canvas 80x80
- 竖向黑色胶囊：颜色圆点（基于编码哈希生成 HSL 色）+ 竖向编码文字
- 打印时 2 列网格，隐藏侧边栏和工具栏

**集采徽章**：
| 类名 | 形状 | 含义 |
|------|------|------|
| `.proc-in` | 绿色圆圈 | 集采范围内 |
| `.proc-out` | 黄色方块 | 集采范围外 |
| `.proc-non` | 红色叉号 | 非集采 |

---

## 8. 信息页面

### 8.1 布局

```
┌────────────────────────────────────────────┐
│  <img src="src/icon/info.svg" width="16"> 信息 │
├────────────────────────────────────────────┤
│                                            │
│  ┌────────────────────────────────────┐    │
│  │        今日邀请码                    │    │
│  │                                    │    │
│  │      X  X  X  X  X  X             │    │
│  │      (大号蓝色，字间距10px)         │    │
│  │      2026-06-07 有效               │    │
│  └────────────────────────────────────┘    │
│                                            │
│  ┌────────────────────────────────────┐    │
│  │  标签内容（始终显示）               │    │
│  │  ┌────┬────┬────┬────┬────┐       │    │
│  │  │名称│编号│类别│型号│品牌│       │    │
│  │  ├────┼────┼────┼────┼────┤       │    │
│  │  │是否│集采│集采│    │    │       │    │
│  │  │集采│时间│范围│    │    │       │    │
│  │  └────┴────┴────┴────┴────┘       │    │
│  └────────────────────────────────────┘    │
│                                            │
│  ┌────────────────────────────────────┐    │
│  │  扫码可见性（可配置）               │    │
│  │  ┌──────┬──────┬──────┬──────┐    │    │
│  │  │ 规格  │ 颜色  │ 材质  │ 描述  │    │    │
│  │  │显示  │ 显示  │ 邀请  │ 敏感  │    │    │
│  │  │ <img src="src/icon/check.svg" width="12" style="color:green"> │ <img src="src/icon/check.svg" width="12" style="color:green"> │ <img src="src/icon/x.svg" width="12" style="color:orange"> │ <img src="src/icon/x.svg" width="12" style="color:red"> │    │    │
│  │  └──────┴──────┴──────┴──────┘    │    │
│  │  ┌──────┐                         │    │
│  │  │ 图片  │                         │    │
│  │  │ 显示  │                         │    │
│  │  │ <img src="src/icon/check.svg" width="12" style="color:green"> │                         │    │
│  │  └──────┘                         │    │
│  └────────────────────────────────────┘    │
└────────────────────────────────────────────┘
```

### 8.2 元素清单

| 区域 | 内容 |
|------|------|
| 今日邀请码 | 大号显示（2.8rem，700 字重，字母间距 10px，主色），日期 + "有效"徽章 |
| 标签内容 | 始终显示的字段网格：名称/编号/类别/型号/品牌/是否集采/集采时间/范围内外 |
| 扫码可见性 | 可配置字段：规格/颜色/材质/描述/图片。每字段三段式按钮：显示<img src="src/icon/check.svg" width="12" style="color:green">/邀请<img src="src/icon/x.svg" width="12" style="color:orange">/敏感<img src="src/icon/x.svg" width="12" style="color:red"> |

### 8.3 交互
- 可见性字段点击循环切换：显示 → 邀请 → 敏感 → 显示
- 立即保存到 `Store` + 同步 Supabase
- 敏感字段使用 CSS `blur(5px)` 模糊，`.revealed` 时清晰

---

## 9. 订单管理

### 9.1 布局

```
┌─────────────────────────────────────────────────┐
│  订单管理                      [<img src="src/icon/arrows-clockwise.svg" width="14"> 刷新] │
├─────────────────────────────────────────────────┤
│ [<img src="src/icon/magnifying-glass.svg" width="14"> 订单号] [全部状态▼] [开始日期] [结束日期] │
│ [查询] [<img src="src/icon/plus.svg" width="14"> 录入] [全选] [<img src="src/icon/trash.svg" width="14"> 批量删除] │
├─────────────────────────────────────────────────┤
│  ┌──────┬──────┬──────┬──────┬──────┬─────┬──┐ │
│  │ <img src="src/icon/x.svg" width="12"> 全选│订单号│下单人│电话  │公司  │项目 │...│ │
│  ├──────┼──────┼──────┼──────┼──────┼─────┼──┤ │
│  │ <img src="src/icon/x.svg" width="12">   │ EO...│ 张三 │138..│ XX   │XX   │..│ │
│  └──────┴──────┴──────┴──────┴──────┴─────┴──┘ │
│  状态徽章：橙色 pending / 绿色 done              │
└─────────────────────────────────────────────────┘
```

### 9.2 工具栏元素清单

| 元素 | ID | 说明 |
|------|-----|------|
| 刷新按钮 | `#refreshOrdersBtn` | 从数据库重新加载订单数据 |
| 搜索输入 | `#orderSearchInput` | 按订单号模糊搜索 |
| 状态下拉 | `#orderStatusFilter` | 全部/未提交/已收录 |
| 开始日期 | `#orderDateStart` | 日期筛选起始 |
| 结束日期 | `#orderDateEnd` | 日期筛选结束 |
| 查询按钮 | `#orderQueryBtn` | 点击触发重新渲染 |
| 录入按钮 | `#createOrderBtn` | 打开录入弹窗 |
| 全选按钮 | `#selectAllOrders` | 全选/取消全选 |
| 批量删除 | `#batchDeleteOrdersBtn` | 批量删除选中订单 |

### 9.3 操作按钮

| 按钮 | 行为 |
|------|------|
| 详情 | 查看订单详情（含明细表） |
| <img src="src/icon/check.svg" width="12">/<img src="src/icon/x.svg" width="12"> 状态切换 | 未提交 ⇄ 已收录，同步数据库 |
| 删除 | 密码验证后删除 |

### 9.4 订单详情明细表（detail-items-table）
`showOrderDetail()` 渲染的明细表包含列：品名、型号、品牌、品类、规格、集采。每行为一个订单项的详细信息。

### 9.5 录入弹窗字段
下单人（必填）、电话（必填）、公司、项目、备注

---

## 10. 申请管理

### 10.1 布局（同订单管理风格）

```
┌─────────────────────────────────────────────────┐
│  申请管理                      [<img src="src/icon/arrows-clockwise.svg" width="14"> 刷新] │
├─────────────────────────────────────────────────┤
│ [<img src="src/icon/magnifying-glass.svg" width="14"> 申请人] [全部事由▼] [全部状态▼] [开始日期] │
│ [结束日期] [查询] [<img src="src/icon/plus.svg" width="14"> 录入] [全选] [<img src="src/icon/trash.svg" width="14"> 批量删除] │
├─────────────────────────────────────────────────┤
│ 表格列：复选框 / 申请人 / 电话 / 公司 / 事由类型  │
│         / 来访日期 / 状态 / 提交时间 / 操作      │
│ 状态徽章：橙色"待审核" / 绿色"已收录"            │
└─────────────────────────────────────────────────┘
```

### 10.2 工具栏元素清单

| 元素 | ID | 说明 |
|------|-----|------|
| 刷新按钮 | `#refreshApplyBtn` | 从数据库重新加载申请数据 |
| 搜索输入 | `#applySearchInput` | 按申请人模糊搜索 |
| 事由类型下拉 | `#applyTypeFilter` | 全部/运输/参观/选样/借还/其他 |
| 状态下拉 | `#applyStatusFilter` | 全部/待审核/已收录 |
| 开始日期 | `#applyDateStart` | 日期筛选起始 |
| 结束日期 | `#applyDateEnd` | 日期筛选结束 |
| 查询按钮 | `#applyQueryBtn` | 点击触发重新渲染 |
| 录入按钮 | `#createApplyBtn` | 打开录入弹窗 |
| 全选按钮 | `#selectAllApply` | 全选/取消全选 |
| 批量删除 | `#batchDeleteApplyBtn` | 批量删除选中申请 |

### 10.3 操作按钮

| 按钮 | 行为 |
|------|------|
| 详情 | 查看申请完整信息 |
| 审核 | 仅"待审核"状态显示，将状态改为"已收录" |
| 删除 | 密码验证后删除 |

### 10.4 申请详情条件字段
根据事由类型不同，`showApplyDetail()` 显示附加字段：
- **运输**：显示车牌号（`licensePlate`）
- **借还**：显示借还类型（`borrowReturnType`）
- **其他**：显示具体事由（`customReason`）

### 10.5 录入弹窗字段
申请人（必填）、电话（必填）、公司、事由类型（下拉：运输/参观/选样/借还/其他）、来访日期、备注

---

## 11. 打卡管理

### 11.1 布局（同订单管理风格）

```
┌─────────────────────────────────────────────────┐
│  打卡管理                      [<img src="src/icon/arrows-clockwise.svg" width="14"> 刷新] │
├─────────────────────────────────────────────────┤
│ [<img src="src/icon/magnifying-glass.svg" width="14"> 姓名] [全部类型▼] [开始日期] [结束日期] │
│ [查询] [<img src="src/icon/plus.svg" width="14"> 录入] [全选] [<img src="src/icon/trash.svg" width="14"> 批量删除] │
├─────────────────────────────────────────────────┤
│ 表格列：复选框 / 姓名 / 电话 / 公司 / 公司类型   │
│         / 打卡位置 / 核验 / 打卡时间 / 操作       │
│ 核验：<img src="src/icon/check.svg" width="14" style="color:green"> 通过（绿色）/ <img src="src/icon/x.svg" width="14" style="color:red"> 未通过（红色） │
└─────────────────────────────────────────────────┘
```

### 11.2 工具栏元素清单

| 元素 | ID | 说明 |
|------|-----|------|
| 刷新按钮 | `#refreshClockBtn` | 从数据库重新加载打卡数据 |
| 搜索输入 | `#clockSearchInput` | 按姓名模糊搜索 |
| 公司类型下拉 | `#clockCompanyTypeFilter` | 全部/业主方/运营方/品牌方/其他 |
| 开始日期 | `#clockDateStart` | 日期筛选起始 |
| 结束日期 | `#clockDateEnd` | 日期筛选结束 |
| 查询按钮 | `#clockQueryBtn` | 点击触发重新渲染 |
| 录入按钮 | `#createClockBtn` | 打开录入弹窗 |
| 全选按钮 | `#selectAllClock` | 全选/取消全选 |
| 批量删除 | `#batchDeleteClockBtn` | 批量删除选中打卡 |

### 11.3 操作按钮

| 按钮 | 行为 |
|------|------|
| 详情 | 查看打卡完整信息 |
| 删除 | 密码验证后删除 |

### 11.4 打卡详情字段
`showClockDetail()` 展示完整信息：姓名、电话、公司、公司类型、打卡时间、打卡位置、位置核验结果（<img src="src/icon/check.svg" width="12" style="color:green">/<img src="src/icon/x.svg" width="12" style="color:red">）、经纬度（`latitude`/`longitude`）、打卡事由、备注。

### 11.5 录入弹窗字段
姓名（必填）、电话（必填）、公司、公司类型（下拉：业主方/运营方/品牌方/其他）、打卡位置、打卡事由

---

## 12. 弹窗系统

### 12.1 项目弹窗（projectModal）

```
┌──────────────────────────────────┐
│  新建类别 / 编辑类别              │
├──────────────────────────────────┤
│  类别名称 [______________]       │
│  品牌     [______________]       │
│  描述     [__________________]   │
│                                  │
│  是否集采： [<img src="src/icon/x.svg" width="12"> 非集采] [<img src="src/icon/check.svg" width="12"> 集采] │  ← vis-pill 按钮组
│  集采时间： [2026]年 [06]月 开始  │  ← 仅集采时显示
│            [2026]年 [12]月 结束  │
├──────────────────────────────────┤
│        [取消]    [保存]          │
└──────────────────────────────────┘
```

### 12.2 样板弹窗（sampleModal）

```
┌──────────────────────────────────┐
│  录入样板 / 编辑样板              │
├──────────────────────────────────┤
│  ┌────────────────────┐          │
│  │  <img src="src/icon/camera.svg" width="24"> 点击拍照或上传 │          │
│  │  （手机端呼出相机）  │          │
│  └────────────────────┘          │
│  品牌 [____]  名称 [____]        │
│  型号 [____]  编号 [0001-SM-...] │
│  集采范围：[范围内] [范围外]      │
│                                  │
│  编辑时额外字段：                 │
│  规格 [____]  颜色 [____]        │
│  材质 [____]                     │
│  描述 [__________________]       │
├──────────────────────────────────┤
│        [取消]    [保存]          │
└──────────────────────────────────┘
```

### 12.3 删除确认弹窗（deleteConfirmModal）

```
┌──────────────────────┐
│  确认删除             │
├──────────────────────┤
│  记录信息描述         │
│                      │
│  密码 [____________]  │
│  （请输入登录密码确认）│
│                      │
│  密码错误提示（隐藏）  │
├──────────────────────┤
│   [取消]  [确认删除]  │
└──────────────────────┘
```

- 调用 Supabase `signInWithPassword` 验证密码
- 验证通过后执行删除（支持：order/apply/clock/sample/project/batch）
- 删除项目时级联删除其所有样板和存储图片

### 12.4 其他弹窗
- **详情弹窗**（`#detailModal`）：通用详情展示，`.detail-grid` 布局
- **录入弹窗**（`_makeEntryModal()`）：动态创建，支持 text/date/select 字段类型
- **标签预览弹窗**（`#labelPreviewModal`）：单标签预览同打印样式
- **裁切弹窗**（`#cropModal`）：全屏深色背景，图片裁切界面

---

## 13. 全局 UI 元素

### 13.1 Toast 通知

| 类型 | 颜色 | 位置 |
|------|------|------|
| success | 绿色 | 右上角固定，z-index: 2000 |
| error | 红色 | 同上 |
| info | 蓝色 | 同上 |
| warning | 橙色 | 同上 |

3 秒后自动淡出，slideIn 动画

### 13.2 图片查看器
- 全屏覆盖（z-index: 10000）
- 深色半透明背景（rgba(0,0,0,0.85)）
- 图片最大 92vw × 92vh
- 点击深色区域关闭

### 13.3 空状态
- flex 居中，min-height 撑满容器
- SVG 图标 + 浅色提示文字
- 不同页面使用不同图标（<img src="src/icon/file.svg" width="16">）

### 13.4 按钮系统

| 类名 | 用途 | 样式 |
|------|------|------|
| `.btn-primary` | 主要操作 | 蓝色填充（`#007aff`），白色文字 |
| `.btn-secondary` | 次要操作 | 浅灰背景/边框，灰色文字 |
| `.btn-danger` | 危险操作 | 红色填充（`#ff3b30`） |
| `.btn-success` | 成功操作 | 绿色填充（`#34c759`） |
| `.btn-ghost` | 幽灵按钮 | 透明背景，灰色文字 |
| `.btn-sm` | 小按钮 | 小内边距和小字号 |
| `.btn-block` | 全宽按钮 | `width: 100%` |
| `.toolbar-btn` | 工具栏按钮 | 高 32px，小圆角 3px，内边距 0 14px |

### 13.5 工具栏元素规格

| 类名 | 规格 |
|------|------|
| `.toolbar-input` | 高 32px，圆角 3px，边框 `#d2d2d7` |
| `.toolbar-select` | 同上，`appearance: auto` |
| `.toolbar-date` | 同上，日期选择器 |
| `.toolbar-btn` | 高 32px，内边距 0 14px |
| `.toolbar-checkbox` | flex 行内，标签+复选框 |

---

## 附录：页面导航流程图

```
登录页
  │
  ▼
仪表盘 ◄────────────────────────────────────────────┐
  │                                                    │
  ├──► 类别（卡片/列表模式）                             │
  │      │                                              │
  │      ├── 查看样板 ──► 样板管理                       │
  │      │                  │                           │
  │      │                  ├── 点击样板卡片 ──► 样板详情 │
  │      │                  ├── 点击"标签" ──► 标签预览   │
  │      │                  ├── 批量打印 ──► 标签打印页   │
  │      │                  └── 录入/编辑/删除样板        │
  │      │                                              │
  │      └── 新建/编辑/删除类别（弹窗）                   │
  │                                                    │
  ├──► 信息（邀请码 + 可见性配置）                        │
  ├──► 订单管理（CRUD + 批量操作）                        │
  ├──► 申请管理（CRUD + 审核 + 批量操作）                  │
  ├──► 打卡管理（CRUD + 批量操作）                        │
  │                                                    │
  └──► 退出 ──► 登录页                                  │
```

---

*本手册基于实际代码（index.html, app.js, style.css）生成，记录了每个页面的外观布局、元素清单和交互逻辑。*
